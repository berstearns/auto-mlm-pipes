# SPLADE EFCAMDAT configs

SPLADE requires query-document paired data (JSONL/CSV with query, positive, negative columns).
The raw EFCAMDAT CSV contains unpaired learner text, so a data preparation step is needed first.

Same data prep approach as ColBERT — see configs/efcamdat/colbert/README.md.
